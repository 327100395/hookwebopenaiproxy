# WebSocket服务器端口
WS_PORT=8080

# API服务器地址
API_HOST=127.0.0.1

# HTTPS配置
# 是否启用HTTPS服务器 (true/false)
ENABLE_HTTPS=true

# SSL证书路径 (相对于项目根目录或绝对路径)
SSL_KEY_PATH=certs/server.key
SSL_CERT_PATH=certs/server.crt

# 生成本地测试证书命令:
# npm run generate-cert